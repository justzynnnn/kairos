"use client";
import { useState } from "react";
import { ConversationPanel } from "@/components/conversation-panel";
import { FriendSearch } from "@/components/friend-search";
import { MeetingInbox } from "@/components/meeting-inbox";
import type { Viewer } from "@/lib/types";
export function InboxWorkspace({viewer,supabaseConfigured}:{viewer:Viewer;supabaseConfigured:boolean}){const[role,setRole]=useState<"justin"|"chloe">("justin");return <div className="space-y-6"><header><p className="eyebrow">Coordination center</p><h1 className="page-title mt-2">Inbox</h1><p className="mt-2 text-[var(--muted)]">Discuss plans, share protected files, and coordinate meetings without exposing private calendar details.</p></header>{!supabaseConfigured&&<section className="card flex flex-wrap items-center justify-between gap-3 p-4"><div><p className="font-display font-semibold text-[var(--navy)]">Demo participant</p><p className="text-xs text-[var(--muted)]">Switch accounts to rehearse messaging and meeting coordination.</p></div><div className="inline-flex rounded-xl bg-[var(--surface-low)] p-1">{(["justin","chloe"]as const).map((value)=><button key={value} onClick={()=>setRole(value)} className={`min-h-10 rounded-lg px-4 text-sm font-semibold capitalize ${role===value?"bg-[var(--navy)] text-white":"text-[var(--muted)]"}`}>{value}</button>)}</div></section>}<FriendSearch/><ConversationPanel role={role} supabaseConfigured={supabaseConfigured}/><MeetingInbox viewer={viewer} supabaseConfigured={supabaseConfigured} role={role}/></div>;}
