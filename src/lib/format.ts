import type{CalendarItem}from"@/lib/types";export function formatTime(v:string|null,tz="Asia/Manila"){return v?new Intl.DateTimeFormat("en-US",{hour:"numeric",minute:"2-digit",timeZone:tz}).format(new Date(v)):""}export function formatDate(v:string|null,tz="Asia/Manila"){return v?new Intl.DateTimeFormat("en-US",{weekday:"short",month:"short",day:"numeric",timeZone:tz}).format(new Date(v)):""}export function isSameLocalDay(v:string|null,d:Date,tz="Asia/Manila"){if(!v)return false;const f=new Intl.DateTimeFormat("en-CA",{year:"numeric",month:"2-digit",day:"2-digit",timeZone:tz});return f.format(new Date(v))===f.format(d)}export function durationLabel(i:CalendarItem){if(!i.startAt||!i.endAt)return null;const m=Math.round((new Date(i.endAt).getTime()-new Date(i.startAt).getTime())/60000);return m<60?`${m}m`:`${Math.floor(m/60)}h${m%60?` ${m%60}m`:""}`}
