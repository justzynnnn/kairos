import {createServerClient} from "@supabase/ssr";import{NextResponse,type NextRequest}from"next/server";import{getSupabasePublicConfig}from"@/lib/env";// These carry their own credentials: a hashed one-time booking token or CRON_SECRET.
const publicPaths=["/auth","/auth/callback","/offline","/manifest.webmanifest","/sw.js","/book","/api/booking","/api/jobs/missed-starts","/api/journey/background"];
export async function proxy(request:NextRequest){const c=getSupabasePublicConfig();if(!c){
  // Without Supabase every request would share one unauthenticated preview identity,
  // so a real deployment must fail closed instead of serving that publicly.
  const deployed=process.env.VERCEL==="1"||process.env.NODE_ENV==="production";
  if(deployed&&process.env.KAIROS_ALLOW_PREVIEW!=="1")return NextResponse.json({error:"Kairos is not configured. Set the Supabase environment variables."},{status:503});
  return NextResponse.next({request})}let response=NextResponse.next({request});const supabase=createServerClient(c.url,c.anonKey,{cookies:{getAll:()=>request.cookies.getAll(),setAll(values){values.forEach(({name,value})=>request.cookies.set(name,value));response=NextResponse.next({request});values.forEach(({name,value,options})=>response.cookies.set(name,value,options))}}});const{data}=await supabase.auth.getUser();const pub=publicPaths.some(p=>request.nextUrl.pathname===p||request.nextUrl.pathname.startsWith(`${p}/`));if(!data.user&&!pub){const u=request.nextUrl.clone();u.pathname="/auth";return NextResponse.redirect(u)}if(data.user&&request.nextUrl.pathname==="/auth"){const u=request.nextUrl.clone();u.pathname="/";return NextResponse.redirect(u)}return response}export const config={matcher:["/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)"]};
