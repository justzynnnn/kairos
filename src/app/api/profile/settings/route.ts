import { NextResponse } from "next/server";import{z}from"zod";import{getProfileSettings,saveProfileSettings}from"@/lib/profile/server";
const time=z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/),schema=z.object({fullName:z.string().trim().min(1).max(80),username:z.string().regex(/^[a-z0-9_]{3,32}$/),timezone:z.string().min(3).max(80),activeStart:time,activeEnd:time,travelBufferMinutes:z.number().int().min(0).max(120),locationEnabled:z.boolean(),automationReminders:z.boolean(),automationLateness:z.boolean(),activityAggregateSharing:z.boolean(),demoMode:z.boolean()}).refine((value)=>value.activeStart<value.activeEnd,{message:"Active hours must end after they start."});
export async function GET(){try{return NextResponse.json({settings:await getProfileSettings()});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Settings could not be loaded."},{status:500});}}
export async function PATCH(request:Request){const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0]?.message??"Profile settings are invalid."},{status:400});try{return NextResponse.json({settings:await saveProfileSettings(parsed.data)});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Settings could not be saved."},{status:422});}}
